@model DocumentationPageViewModel

@{
    ViewData["Title"] = Model.Current.Title;
    Layout = "_AuthenticatedLayout";
}

@section Styles {
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/highlightjs/cdn-release@11/build/styles/github.min.css" />
    <style>
        .docs-markdown {
            --docs-heading: #020617;
            --docs-body: #475569;
            --docs-muted: #64748b;
            --docs-border: #e2e8f0;
            --docs-panel: #f8fafc;
            --docs-inline-code-bg: #f1f5f9;
            --docs-inline-code-text: #0f172a;
            --docs-note-bg: #fffbeb;
            --docs-note-border: #f59e0b;
            --docs-note-text: #78350f;
            --docs-link: #b45309;
        }

        .dark .docs-markdown {
            --docs-heading: #f8fafc;
            --docs-body: #cbd5e1;
            --docs-muted: #94a3b8;
            --docs-border: #334155;
            --docs-panel: #0f172a;
            --docs-inline-code-bg: rgba(51, 65, 85, .7);
            --docs-inline-code-text: #e2e8f0;
            --docs-note-bg: rgba(245, 158, 11, .12);
            --docs-note-border: rgba(245, 158, 11, .75);
            --docs-note-text: #fcd34d;
            --docs-link: #fbbf24;
        }

        .docs-markdown h1,
        .docs-markdown h2,
        .docs-markdown h3 {
            color: var(--docs-heading);
            font-weight: 700;
            line-height: 1.25;
            scroll-margin-top: 24px;
        }

        .docs-markdown h1 {
            font-size: 1.875rem;
            margin-bottom: 1rem;
        }

        .docs-markdown h2 {
            border-top: 1px solid var(--docs-border);
            font-size: 1.25rem;
            margin-top: 2rem;
            padding-top: 1.5rem;
        }

        .docs-markdown h3 {
            font-size: 1rem;
            margin-top: 1.5rem;
        }

        .docs-markdown p,
        .docs-markdown li {
            color: var(--docs-body);
            font-size: .9375rem;
            line-height: 1.7;
        }

        .docs-markdown p {
            margin-top: .75rem;
        }

        .docs-markdown ul,
        .docs-markdown ol {
            margin-top: .75rem;
            padding-left: 1.25rem;
        }

        .docs-markdown ul {
            list-style: disc;
        }

        .docs-markdown ol {
            list-style: decimal;
        }

        .docs-markdown blockquote {
            border-left: 4px solid var(--docs-note-border);
            margin-top: 1rem;
            padding: .75rem 1rem;
            background: var(--docs-note-bg);
            border-radius: .375rem;
        }

        .docs-markdown blockquote p {
            color: var(--docs-note-text);
            font-weight: 600;
            margin: 0;
        }

        .docs-markdown a {
            color: var(--docs-link);
            font-weight: 600;
            text-decoration: underline;
            text-underline-offset: .2em;
        }

        .docs-markdown table {
            border-collapse: collapse;
            display: block;
            margin-top: 1rem;
            overflow-x: auto;
            width: 100%;
        }

        .docs-markdown th,
        .docs-markdown td {
            border: 1px solid var(--docs-border);
            padding: .75rem;
            text-align: left;
            vertical-align: top;
        }

        .docs-markdown th {
            background: var(--docs-panel);
            color: var(--docs-heading);
            font-size: .8125rem;
            font-weight: 700;
            text-transform: uppercase;
        }

        .docs-markdown td {
            color: var(--docs-body);
            font-size: .875rem;
        }

        .docs-markdown code {
            background: var(--docs-inline-code-bg);
            border-radius: .25rem;
            color: var(--docs-inline-code-text);
            font-size: .85em;
            padding: .15rem .35rem;
        }

        .docs-markdown pre {
            background: var(--docs-panel);
            border: 1px solid var(--docs-border);
            border-radius: .5rem;
            color: var(--docs-inline-code-text);
            margin-top: 1rem;
            overflow-x: auto;
            padding: 1rem;
        }

        .docs-markdown pre code {
            background: transparent;
            color: inherit;
            padding: 0;
        }

        .docs-markdown .hljs {
            background: transparent;
            color: inherit;
        }

        .dark .docs-markdown .hljs-comment,
        .dark .docs-markdown .hljs-quote {
            color: #94a3b8;
        }

        .dark .docs-markdown .hljs-keyword,
        .dark .docs-markdown .hljs-selector-tag,
        .dark .docs-markdown .hljs-subst {
            color: #93c5fd;
        }

        .dark .docs-markdown .hljs-literal,
        .dark .docs-markdown .hljs-number,
        .dark .docs-markdown .hljs-variable,
        .dark .docs-markdown .hljs-template-variable {
            color: #fca5a5;
        }

        .dark .docs-markdown .hljs-string,
        .dark .docs-markdown .hljs-doctag,
        .dark .docs-markdown .hljs-attr,
        .dark .docs-markdown .hljs-symbol,
        .dark .docs-markdown .hljs-bullet {
            color: #86efac;
        }

        .dark .docs-markdown .hljs-title,
        .dark .docs-markdown .hljs-section,
        .dark .docs-markdown .hljs-name {
            color: #fbbf24;
        }
    </style>
}

<section class="space-y-6">
    <div class="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between">
        <div>
            <p class="text-sm font-medium text-amber-700 dark:text-amber-400">Ajuda</p>
            <h2 class="mt-2 text-2xl font-semibold text-slate-950 dark:text-slate-100">@Model.Current.Title</h2>
            <p class="mt-2 max-w-2xl text-sm text-slate-600 dark:text-slate-300">@Model.Current.Description</p>
        </div>

        <a asp-controller="Docs" asp-action="Index" class="inline-flex h-10 items-center justify-center rounded-md border border-slate-300 dark:border-slate-700 px-4 text-sm font-medium text-slate-700 dark:text-slate-300 transition hover:bg-slate-50 dark:hover:bg-slate-800">
            Todos os guias
        </a>
    </div>

    <div class="grid gap-6 xl:grid-cols-[280px_1fr]">
        <aside class="hidden xl:block">
            <div class="sticky top-6 space-y-4">
                <div class="rounded-lg border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 p-4 shadow-sm">
                    <p class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Guias</p>
                    <nav class="mt-3 space-y-1">
                        @foreach (var item in Model.Items.OrderBy(item => item.Order).ThenBy(item => item.Title))
                        {
                            var itemClass = item.Slug == Model.Current.Slug
                                ? "block rounded-md bg-amber-50 dark:bg-amber-500/10 px-2 py-1.5 text-sm font-medium text-amber-900 dark:text-amber-200"
                                : "block rounded-md px-2 py-1.5 text-sm font-medium text-slate-600 dark:text-slate-300 transition hover:bg-slate-50 dark:hover:bg-slate-800 hover:text-slate-950 dark:hover:text-slate-100";
                            <a asp-controller="Docs" asp-action="Show" asp-route-slug="@item.Slug" class="@itemClass">@item.Title</a>
                        }
                    </nav>
                </div>

                <div class="rounded-lg border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 p-4 shadow-sm">
                    <p class="text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Nesta página</p>
                    <nav id="docs-toc" class="mt-3 space-y-1"></nav>
                </div>
            </div>
        </aside>

        <article class="rounded-lg border border-slate-200 dark:border-slate-800 bg-white dark:bg-slate-900 p-6 shadow-sm">
            <div id="docs-loading" class="flex items-center gap-3 text-sm text-slate-500 dark:text-slate-400">
                <i class="ph ph-circle-notch animate-spin text-xl" aria-hidden="true"></i>
                Carregando guia...
            </div>
            <div id="docs-content" class="docs-markdown hidden" data-markdown-url="@Url.Action("Markdown", "Docs", new { slug = Model.Current.Slug })"></div>
        </article>
    </div>
</section>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/marked@14/marked.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/dompurify@3/dist/purify.min.js"></script>
    <script src="https://cdn.jsdelivr.net/gh/highlightjs/cdn-release@11/build/highlight.min.js"></script>
    <script>
        (async function () {
            const content = document.getElementById('docs-content');
            const loading = document.getElementById('docs-loading');
            const toc = document.getElementById('docs-toc');

            function slugify(value) {
                return value
                    .toLowerCase()
                    .normalize('NFD')
                    .replace(/[\u0300-\u036f]/g, '')
                    .replace(/[^a-z0-9]+/g, '-')
                    .replace(/^-+|-+$/g, '');
            }

            try {
                const response = await fetch(content.dataset.markdownUrl, {
                    headers: { 'Accept': 'text/markdown' }
                });

                if (!response.ok) {
                    throw new Error(`HTTP ${response.status}`);
                }

                marked.setOptions({ gfm: true, breaks: false });

                const markdown = await response.text();
                content.innerHTML = DOMPurify.sanitize(marked.parse(markdown));

                content.querySelectorAll('pre code').forEach((block) => hljs.highlightElement(block));

                content.querySelectorAll('h2, h3').forEach((heading) => {
                    const id = slugify(heading.textContent || 'secao');
                    heading.id = id;

                    if (!toc) {
                        return;
                    }

                    const link = document.createElement('a');
                    link.href = `#${id}`;
                    link.textContent = heading.textContent;
                    link.className = heading.tagName === 'H2'
                        ? 'block rounded-md px-2 py-1.5 text-sm font-medium text-slate-700 dark:text-slate-300 transition hover:bg-slate-50 dark:hover:bg-slate-800 hover:text-slate-950 dark:hover:text-slate-100'
                        : 'block rounded-md px-4 py-1 text-xs text-slate-500 dark:text-slate-400 transition hover:bg-slate-50 dark:hover:bg-slate-800 hover:text-slate-900 dark:hover:text-slate-100';
                    toc.appendChild(link);
                });

                loading.classList.add('hidden');
                content.classList.remove('hidden');
            } catch (error) {
                loading.classList.remove('text-slate-500');
                loading.classList.add('text-red-700');
                loading.innerHTML = 'Não foi possível carregar o guia.';
                console.error(error);
            }
        })();
    </script>
}
